import React from "react";

const Footer = () => (
  <footer className="text-center mt-5 mb-4 text-muted">
    <small>&copy; 2025 Marvel Character DB — Built with Flask & React</small>
  </footer>
);

export default Footer;
